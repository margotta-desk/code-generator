export interface I<%-Model.ClassName%>Params {
<%
	if (Model.Key) {
		Model.Key.Properties.forEach((property) => {
%>	<%-property.PropertyName%><%- property.Nullable ? '?': '' %>: <%-property.PropertyType%>
<%
		})
	}
%>}