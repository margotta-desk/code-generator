<%-Imports.trim()%>

@Module({
	imports:
		[
			TypeOrmModule.forFeature([
				<%= Model.Classes.map(m => `${m.ClassName}Model,`).join('\n\t\t\t\t') %>
			])
		],
	controllers:
		[
<% 
		Model.Classes.forEach((model, index) => {
%>			// <%-model.ClassName%>Controller,
<%
		})
%>		],
	providers:
		[
			//	Repositories
<% 
		Model.Classes.forEach((model, index) => {
%>			<%-model.ClassName%>Repository,
<%
		})
%>
			//	Services
<% 
		Model.Classes.forEach((model, index) => {
%>			<%-model.ClassName%>Service,
<%
		})
%>		],
	exports:
		[
			//	Repositories
<% 
		Model.Classes.forEach((model, index) => {
%>			<%-model.ClassName%>Repository,
<%
		})
%>
			//	Services
<% 
		Model.Classes.forEach((model, index) => {
%>			<%-model.ClassName%>Service,
<%
		})
%>		]
})
export class <%-Model.ModuleName%>Module { }